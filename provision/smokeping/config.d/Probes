*** Probes ***

+ DNS

binary = /usr/bin/dig

+ FPing

binary = /usr/sbin/fping

+ FPing6

binary = /usr/sbin/fping
protocol = 6

+ TCPPing

binary = /usr/bin/tcpping

+ TraceroutePing

binary = /usr/bin/traceroute
binaryv6 = /usr/bin/traceroute6
